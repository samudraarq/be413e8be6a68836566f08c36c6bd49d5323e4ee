import styled from "styled-components";

const Wrapper = styled.div`
  border-bottom: 1px solid #f1f1f2;
`;

const DatePicker = () => {
  return <Wrapper>From date picker</Wrapper>;
};

export default DatePicker;
