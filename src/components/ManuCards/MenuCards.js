import styled from "styled-components";
import Container from "../UI/Container";

const Wrapper = styled.div`
  padding-top: 13.7rem;
`;

const MenuCards = () => {
  return (
    <Container>
      <Wrapper>From menu container</Wrapper>
    </Container>
  );
};

export default MenuCards;
